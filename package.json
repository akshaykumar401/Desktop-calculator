{
  "name": "desktop_calculator",
  "version": "1.0.0",
  "description": "Desktop Calculator Application",
  "main": "main.js",
  "scripts": {
    "pack": "electron-packager . Calculator --platform=win32 --arch=x64 --out=dist/ --overwrite",
    "dist": "npm run pack && npm run create-installer",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node"
  ],
  "author": "Akshay Kumar",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron": "^34.2.0"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.6",
    "mathjs": "^14.2.1",
    "tailwindcss": "^4.0.6"
  }
}
